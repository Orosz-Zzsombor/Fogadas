<Window x:Class="FogadasMokuskodas.ModifyUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modify User" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Username}" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Email:" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Email}" Grid.Row="3" Margin="0,0,0,10"/>

        <TextBlock Text="Balance:" Grid.Row="4" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Balance}" Grid.Row="5" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="75" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>